# ─── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# ─── Virtual envs ───────────────────────────────────────
venv/
.venv/
env/

# ─── Database & runtime state ───────────────────────────
*.db
*.sqlite
*.sqlite3
jobpilot.db
jobpilot.db.bak-*
*.db.bak-*
logs/

# ─── Generated artifacts ────────────────────────────────
output/
*.bak
*.docx.bak

# ─── Personal data (sanitized examples committed) ──────
# settings.yaml carries the `user:` block printed on résumé/cover-letter
# letterheads — real name, email and phone. Tune settings here; the committed
# copy is config/settings.example.yaml.
config/settings.yaml
config/applicant_profile.yaml
config/base_resume.yaml
config/base_resume_bt.yaml
config/companies_seed.yaml
config/agent_preferences.yaml

# ─── Credentials & secrets ──────────────────────────────
credentials.json
token.json
*.json.bak
config/google_credentials.json
config/gmail.yaml
.env
.env.local

# ─── IDE / editor / OS ──────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# ─── Claude Code / planning workspace ───────────────────
.claude/
.planning/

# ─── Playwright ─────────────────────────────────────────
playwright-report/
test-results/
config/*.pdf
config/*.meta.json

# Personal data pulled from JobRight (2026-09-07) and dashboard QA screenshots — never publish
config/jobright_applied_export.json
tests/qa-screenshots/*.png
tests/qa-screenshots/*.pdf
tests/qa-screenshots/*.docx
